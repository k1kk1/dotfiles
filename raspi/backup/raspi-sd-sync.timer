# raspi: 毎週日曜 4 時に microSD の予備環境を更新する。電源が切れていた場合は次の起動後に実行
[Unit]
Description=Weekly sync of NVMe to the microSD fallback system

[Timer]
OnCalendar=Sun *-*-* 04:00:00
RandomizedDelaySec=30min
Persistent=true

[Install]
WantedBy=timers.target
